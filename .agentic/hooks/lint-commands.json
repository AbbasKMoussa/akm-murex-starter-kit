{
  "_comment": "Map a file extension (no dot) to a structured lint command. command must be a bare executable name; args must be an array of strings containing at least one {file} placeholder. Commands run directly without a shell. Add real top-level keys like \"py\" to activate. The _examples block is illustrative only and is not used.",
  "_examples": {
    "py": { "command": "ruff", "args": ["check", "{file}"] },
    "ts": { "command": "npx", "args": ["--no-install", "eslint", "{file}"] },
    "js": { "command": "npx", "args": ["--no-install", "eslint", "{file}"] },
    "go": { "command": "gofmt", "args": ["-l", "{file}"] }
  }
}
