{
  "_comment": "Map a file extension (no dot) to a lint command for lint-on-edit. Use {file} as the path placeholder. Add real top-level keys like \"py\" to activate. Empty of active entries by default, so lint-on-edit is a no-op until configured. The _examples block is illustrative only and is NOT used (lint-on-edit looks up the extension as a top-level key).",
  "_examples": {
    "py": "ruff check {file}",
    "ts": "npx eslint {file}",
    "js": "npx eslint {file}",
    "go": "gofmt -l {file}"
  }
}
