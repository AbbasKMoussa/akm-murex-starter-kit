# AGENTS.md

> This is a placeholder created by AKMaestro. Run `/init` (the
> initialization flow) to replace it with real, repo-specific instructions. The
> `setup-instructions` skill fills in the sections below from detected facts and
> your answers.

## Product

<run `/init` to fill in: what this product is and does>

## Build

<build command>

## Tests

<test command>

## Run

<how to run/serve the app locally, if applicable>

## Verify a Change

<how a developer confirms a change works>

## Agent Rules

- Keep changes scoped to the requested task.
- Prefer existing patterns in the touched area.
- Run the relevant build or test command before reporting done when feasible.
- Do not overwrite instructions or workflow files without approval.
